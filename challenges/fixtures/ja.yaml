# challenges/fixtures/ja.yaml
#
# 日本語 locale fixture for the Recovery describe challenge.
# Verifies CONST-046 i18n posture for CJK glyph handling — banner
# must round-trip through YAML parse + locale dispatch without
# encoding corruption.

locale: "ja"
banner: "=== Recovery 記述チャレンジ (JA) ==="
breaker.label: "サーキットブレーカー"
breaker.success: "閉状態の成功: ペイロード=%s"
breaker.tripped: "%d回の失敗後にブレーカーがトリップ: 状態=%s"
breaker.recovered: "ブレーカーはリセット後に %s に回復"
health.label: "ヘルスチェッカー"
health.healthy: "健全なプローブを観測 (状態=%s, エラー=%v)"
health.unhealthy: "不健全なプローブを観測 (状態=%s, エラー=%v)"
facade.label: "レジリエンスファサード"
facade.executed: "facade.Execute は次を返した: エラー=%v"
facade.stats: "facade.Stats のキー: %v"
result.success: "Recovery プリミティブはエンドツーエンドで動作確認済み。"
result.failure: "Recovery プリミティブの後退を検出; 調査せよ。"
expect.breaker.name: "round-280-breaker"
expect.health.name: "round-280-health"
expect.facade.endpoint: "round-280-endpoint"
